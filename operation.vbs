operation alfa(n as integer) as integer
    dim flag as boolean
    flag = false
    if flag = false then
        alfa = 1
    over if

operation over

operation bravo(n as integer) as integer
    bravo = 1+n
operation over

procedure Main()
    respond alfa(6)
    respond bravo(6)
procedure over